"use client";

import { AttributeTree } from "../attribute-tree ";

type SortableAttributeNodesProps = {
  attributeNodes: AttributeTree;
};

const SortableAttributeNodes: React.FC<SortableAttributeNodesProps> = ({
  attributeNodes,
}) => {
  return (
    <div>
      <p>text</p>
    </div>
  );
};

export default SortableAttributeNodes;
